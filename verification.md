# UI Verification Notes

## 2026-08-25

- 未ログインで `/workspace` を開いた際も、ロゴ、トップ、ブロック図鑑、保存プロジェクト、ログインへの導線が表示されることを確認した。
- モバイル幅では、設計開始、図鑑、保存プロジェクト、管理（管理者の場合）のナビゲーションを表示する実装にした。

## 提供画像による通し検証

- 提供されたアルセウス画像をサーバー側マルチモーダルAIで解析し、建築適性72点、部位6件、建築レイヤー3件を取得した。
- 候補選択から100ブロック高の設計図を生成し、2D設計図は254セル、3Dプレビューは127ボクセルとして保存・再読込を確認した。
- 内容を含む2D・3D PNGを作成してオブジェクトストレージへ保存し、保存済みプロジェクトに紐付くURLを確認した。

## 3D形状の改善

- 乱数的な高さを付与していた旧方式を、部位名に基づく楕円体・梁・前面リングの組み合わせへ変更した。
- 提供画像の検証プロジェクトは、頭部・角・首・胴体・四脚・尻尾・金色の輪を別部位として持つ182ボクセルの3D設計データに再生成した。

## 2026-08-26: 多色化とレイヤー別2D設計図

- 提供画像の正面・左右側面を統合し、白・濃灰・金・緑を近似ブロックへ割り当てた多色の2D／3D設計図を生成した。ユーザーが画面上で配色を確認した。
- 2D設計図に、全体と各レイヤーを切り替える表示、ブロック名・個数・全配置座標の一覧、ホバー時のブロック情報を追加した。
- ユーザーがレイヤー切替後にPNG保存を行い、保存済み2D PNGに選択レイヤーの配置図と座標一覧が反映されることを確認した。

## 2026-08-26: 保存済み設計図の再訪

- 提供画像プロジェクトの設計スタジオを新規に開き直し、保存済みの多色2Dレイヤー図、選択済みブロック候補、レイヤー切替が初期表示されることをデスクトップ幅で確認した。
- 同じ保存済み設計図をモバイル幅でも開き直し、候補選択と設計図への導線が崩れずに表示されることを確認した。
- `?view=3d` を指定して保存済みプロジェクトを再訪し、保存済みの多色3Dボクセル設計図と素材一覧がデスクトップ幅で初期表示されることを確認した。
- 同じ3D再訪URLをモバイル幅でも確認し、画面幅に応じた候補選択一覧と3D設計図への導線が表示されることを確認した。

## 2026-08-26: 提供ブロックカタログの取込

- `block.json` は175ブロック、4カテゴリ（カベ、ゆか・どうろ、鉱石・特殊、地形・環境）、27素材で構成され、キー・名称の重複はなかった。`estimated_block_counts.json` の30ブロックキーはすべてカタログ内に存在した。
- 対応画像を持つ172ブロックをストレージとDBへ取り込み、27素材と148件の素材内訳を登録した。図鑑で取り込んだブロック画像、名称、カテゴリ、画像代表色が表示されることを確認した。
- `ねんど`、`きんこうせき`、`メタモンだらけもよう` の3ブロックは対応画像が提供されていないため、誤った画像・色で登録しないよう保留した。
- 画像の白背景を除外してブロック本体の代表色を再抽出し、172ブロックの色を再同期した。ストレージ画像を重複アップロードせず、既存カタログ行だけを更新した。
- 提供JSONの入手方法・解放条件から8件の関連地点を抽出し、27素材へ関連地点を設定した。これはブロックの入手・解放情報に基づく案内であり、素材自体の直接採取場所を保証するものではない。
- 実画像プロジェクトの部位色を対象に、提供カタログ172ブロックの近似候補を検証した。頭部などの白系部位には「ぬののカベ」、金系部位には「木のカベ・ライト」など、提供画像を持つブロックが候補として選定可能であることを確認した。
- プロジェクト1の候補を更新し、6部位すべてに提供カタログ由来の候補を追加した。保存済み凡例から既存選択を復元して、6部位すべての選択状態を維持した。
- 同プロジェクトの多色2D／3D設計図を提供カタログを含む全パレットで再生成した。303セル中270セル、735ボクセル中636ボクセルに提供カタログ由来のブロックが割り当てられた。
